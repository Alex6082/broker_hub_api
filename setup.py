from setuptools import setup, find_packages

setup(
    name='broker_hub_api',
    version='1',
    url='https://github.com/Alex6082/broker_hub_api/',
    packages=find_packages(),
    install_requires=[
        "requests",
    ])
